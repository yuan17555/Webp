import TextField from '@mui/material/TextField';

function EM(){
    return(
        <TextField
            // margin="normal"
            // required
            // fullWidth
            // id="email"
            // label="Email Address"
            // name="email"
            // autoComplete="email"
            // autoFocus
            margin="normal"
            required
            fullWidth
            name="email"
            label="Email Address"
            type="input"
            id="email"
            autoComplete="email"
        />
    );
}
export default EM;